# You can not change tuples
numbers = (1,2,3) # This is a tuple, tuples have parenthesis instead of []
# Tuples dont have anything that change them like remove, clear or index for example

print(numbers[2])